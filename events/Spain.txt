add_namespace = spe

# Spanish Civil War
country_event = {
	id = spe.1
	title = spe.1.title
	desc = spe.1.desc
	picture = GFX_report_event_spain_civil_war_soldiers
	is_triggered_only = no
	fire_only_once = yes
	
	trigger = {
		NOT = { has_global_flag = scw_over }
		tag = SPE
		date > 1936.1.1
	}
	
	immediate = {
		hidden_effect = {
			SPE = { declare_war_on = { target = FSP type = annex_everything } }
			news_event = { hours = 6 id = news.63 }
		}
	}
	option = {
		name = spe.1.a
		
	}
}